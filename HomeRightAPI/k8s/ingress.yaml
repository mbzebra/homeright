apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homeright-api
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: homeright-api
                port:
                  number: 8000

