apiVersion: v1
kind: Secret
metadata:
  name: homeright-secrets
type: Opaque
stringData:
  # Change these before applying to any shared/production cluster.
  MONGO_INITDB_ROOT_USERNAME: homeright
  MONGO_INITDB_ROOT_PASSWORD: homeright_password_change_me

