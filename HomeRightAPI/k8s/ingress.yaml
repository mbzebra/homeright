apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homeright-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: homeright-api
                port:
                  number: 8000

